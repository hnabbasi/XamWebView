<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamWebView.MainPage"
             xmlns:local="clr-namespace:XamWebView">
    <ScrollView Padding="0">
        <StackLayout Padding="0"
                     Spacing="0">
            <Frame Padding="16,70,16,16">
                <Label Text="Welcome"
                       Style="{StaticResource Title}"/>
            </Frame>

            <local:MyWebView HeightRequest="500"
                     Source="{StaticResource MeetupPage}"/>

            <Frame>
                <Label Text="Copyright 2021"
                       Style="{StaticResource Footer}"
                       HorizontalOptions="Center"/>
            </Frame>
        </StackLayout>
    </ScrollView>
</ContentPage>
